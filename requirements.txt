# Core data
requests
pandas
pyarrow
numpy
pyyaml
python-dotenv
tqdm
joblib

# Market / calendar / news
yfinance
exchange-calendars          # market-hours helper — lead time is measured in
                            # trading hours, not wall-clock hours
finnhub-python

# RL / sequential decision
gymnasium
stable-baselines3

# Baselines + evaluation (CUSUM is hand-written, no dependency)
scikit-learn
xgboost
scipy

# torch: install separately with the CUDA index-url for the RTX 3050, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# Demo + tracking
streamlit
plotly
tensorboard

# Tests
pytest
