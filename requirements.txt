# Core data
requests
zstandard
duckdb
pandas
pyarrow
numpy
pyyaml
python-dotenv
tqdm
joblib

# Market / news
yfinance
pandas-market-calendars
finnhub-python

# NLP feature extraction (frozen, offline)
sentence-transformers
transformers
# torch: install separately with the CUDA index-url for the RTX 3050, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# RL
gymnasium
stable-baselines3

# Baselines / eval
scikit-learn
xgboost

# LLM triage + policy baseline (free tiers)
google-generativeai
groq

# Demo + tracking
streamlit
plotly
tensorboard

# Tests
pytest
