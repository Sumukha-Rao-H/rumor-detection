# Core data
requests
pandas
pyarrow
numpy
pyyaml
python-dotenv
tqdm

# Market / calendar / news
yfinance
# Pinned exactly, unlike everything else here. This package ships holiday
# tables and early-close rules as DATA: an upgrade can move a session boundary,
# which silently changes every lead-time number in the report with no error and
# no test failure. `>=` does not help — it is the upgrade that breaks it.
exchange-calendars==4.13.2  # market-hours helper — lead time is measured in
                            # trading hours, not wall-clock hours
# NOTE: no finnhub-python. src/collectors/news.py calls the REST endpoint with
# `requests` so pacing stays under our own RateLimiter (news.finnhub_min_interval_s).
# The SDK would duplicate that and hide the rate limit. Do not re-add it.

# RL / sequential decision
gymnasium
stable-baselines3

# Baselines + evaluation (CUSUM is hand-written, no dependency)
scikit-learn
xgboost
scipy

# torch: install separately with the CUDA index-url for the RTX 3050, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# Demo + tracking
streamlit
plotly
tensorboard

# Tests
pytest
